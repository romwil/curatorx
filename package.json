{
  "name": "curatorx",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "python -m unittest discover -s tests -q && npm run test:frontend && npm run test:e2e",
    "test:frontend": "cd frontend && npm install && npm run build",
    "test:e2e": "playwright test",
    "test:e2e:live": "E2E_MOCK_APIS=0 playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
